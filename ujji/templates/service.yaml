apiVersion: v1
kind: Service
metadata:
  name: {{ include "ujji.fullname" . }}
  labels:
    {{- include "ujji.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ujji.selectorLabels" . | nindent 4 }}
