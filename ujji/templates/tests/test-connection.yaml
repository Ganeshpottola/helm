apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ujji.fullname" . }}-test-connection"
  labels:
    {{- include "ujji.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ujji.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
